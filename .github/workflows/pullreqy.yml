on:
  pull_request:
    branches: [ main ]

jobs:
    build:
      runs-on: ubuntu-latest
      steps:
        - name: Checkout 📁
          uses: actions/checkout@v1
        - run: |
           echo was runn sine we did create a pull_request wf for it
